{
  "buildCommand": "cd web && npm install && npm run build && cd ../server && npm install && npx prisma generate && npx prisma migrate deploy && npx tsx prisma/seed.ts",
  "outputDirectory": "web/dist",
  "functions": {
    "server/api/index.ts": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/server/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
